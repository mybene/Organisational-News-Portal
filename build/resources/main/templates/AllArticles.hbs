{{# partial "content"}}
    <div class="articles">
        {{# if article}}
            {{each article}}
            <h4>{{title}}</h4>
            <p>{{contnet}}</p>

        {{/if}}

    </div>

{{/partial}}
{{>layout.hbs}}